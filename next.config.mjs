/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["ttdk-sandbox-api.service.makefamousapp.com","noname1412.ap-south-1.linodeobjects.com"],
  },
};

export default nextConfig;
